export const applicationSearchAbleFields: string[] = ["status"]; //* only for search term

export const applicationFilterableFields: string[] = ["status", "searchTerm"]; //* for all filtering

export const ApplicationStatus = ['PENDING','ACCEPTED'];